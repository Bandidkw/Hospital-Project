# NewsImageDetailView - หน้ารายละเอียดข่าวสารแบบใหม่

## ภาพรวม

หน้า `NewsImageDetailView.vue` เป็นหน้าแสดงรายละเอียดข่าวสารที่ออกแบบใหม่ให้สวยงาม น่าเชื่อถือ และรองรับทุกอุปกรณ์ เหมาะสำหรับข่าวสารที่เป็นรูปภาพหรือมีเนื้อหาที่ต้องการการนำเสนอที่โดดเด่น

## คุณสมบัติหลัก

### 🎨 การออกแบบ

- **Modern UI/UX**: ใช้ Gradient backgrounds, Card-based layout, และ Smooth animations
- **Responsive Design**: รองรับทุกขนาดหน้าจอ (Mobile, Tablet, Desktop)
- **Typography**: ใช้ฟอนต์ที่อ่านง่าย พร้อม Hierarchy ที่ชัดเจน
- **Color Scheme**: ใช้สีที่สร้างความน่าเชื่อถือ (Blue, Gray tones)

### 📱 การรองรับอุปกรณ์

- **Mobile First**: ออกแบบเริ่มจาก Mobile แล้วขยายไป Desktop
- **Touch Friendly**: ปุ่มและ Interactive elements ขนาดเหมาะสมสำหรับการสัมผัส
- **Performance Optimized**: โหลดเร็ว ใช้ Lazy loading สำหรับรูปภาพ

### 🚀 ฟีเจอร์พิเศษ

#### Navigation & UX

- **Sticky Navigation**: แถบนำทางติดด้านบนเมื่อเลื่อนหน้า
- **Smart Back Button**: กลับไปหน้าก่อนหน้า หรือหน้าข่าวหากไม่มี History
- **Breadcrumb**: แสดงเส้นทางการนำทางที่ชัดเจน

#### Content Display

- **Hero Image**: แสดงรูปภาพหลักแบบ Full-width พร้อม Aspect ratio ที่เหมาะสม
- **Reading Time**: คำนวณเวลาอ่านโดยประมาณ
- **View Counter**: แสดงจำนวนผู้เข้าชม (รองรับ K, M format)
- **Category Badge**: แสดง Badge หมวดหมู่ที่สวยงาม
- **Tags System**: แสดงแท็กที่เกี่ยวข้อง

#### Interactive Features

- **Share Function**: แชร์ผ่าน Native Share API หรือ Copy link
- **Print Support**: พิมพ์เอกสารพร้อม Print-optimized styles
- **PDF Download**: ดาวน์โหลดเอกสาร PDF หากมี
- **Image Zoom**: รองรับการซูมรูปภาพ (ในอนาคต)

#### Error Handling

- **Graceful Loading**: แสดง Skeleton loading ขณะโหลดข้อมูล
- **Error States**: แสดงข้อความข้อผิดพลาดที่เป็นมิตร
- **Image Fallback**: แสดงรูป Placeholder เมื่อโหลดรูปไม่สำเร็จ
- **Retry Mechanism**: ปุ่มลองใหม่เมื่อเกิดข้อผิดพลาด

## การใช้งาน

### Route Configuration

```typescript
{
  path: '/news/:id/image',
  name: 'news-image-detail',
  component: () => import('@/views/public/NewsImageDetailView.vue'),
  meta: { requiresAuth: false },
}
```

### การเรียกใช้

```vue
<!-- จากหน้าข่าวสาร -->
<router-link :to="{ name: 'news-image-detail', params: { id: news.id } }">
  อ่านต่อ
</router-link>

<!-- หรือใช้ JavaScript -->
router.push({ name: 'news-image-detail', params: { id: newsId } })
```

### Props & Data Structure

หน้านี้ใช้ข้อมูลจาก `NewsDetailItem` interface:

```typescript
interface NewsDetailItem {
  id: string
  title: string
  content: string
  date: string
  category?: string
  imageUrl?: string | null
  pdfUrl?: string | null
  views?: number
  tags?: string[]
  // ... other fields
}
```

## การปรับแต่ง

### สี Theme

แก้ไขสีใน Tailwind classes:

- Primary: `blue-600`, `blue-700`
- Secondary: `gray-600`, `gray-700`
- Accent: `red-600` (สำหรับ PDF), `green-600` (สำหรับ Print)

### Layout

- Container: `max-w-4xl` (ปรับได้ตามต้องการ)
- Spacing: ใช้ Tailwind spacing scale
- Typography: ใช้ `prose` classes สำหรับเนื้อหา

### Responsive Breakpoints

- Mobile: `< 640px`
- Tablet: `640px - 1024px`
- Desktop: `> 1024px`

## Performance Considerations

### Image Optimization

- ใช้ `loading="lazy"` สำหรับรูปภาพ
- รองรับ WebP format
- Responsive images ด้วย `srcset` (ในอนาคต)

### Code Splitting

- Component ถูก Lazy load ผ่าน `import()`
- แยก CSS สำหรับ Print styles

### SEO Friendly

- Semantic HTML structure
- Meta tags support (ในอนาคต)
- Open Graph tags (ในอนาคต)

## Browser Support

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+
- Mobile browsers (iOS Safari, Chrome Mobile)

## การพัฒนาต่อ

### ฟีเจอร์ที่สามารถเพิ่มได้

1. **Related News**: แสดงข่าวที่เกี่ยวข้อง
2. **Comments System**: ระบบความคิดเห็น
3. **Social Media Integration**: แชร์ไปยัง Social platforms
4. **Reading Progress**: แสดงความคืบหน้าการอ่าน
5. **Dark Mode**: โหมดมืด
6. **Accessibility**: ปรับปรุง ARIA labels และ Keyboard navigation

### การทดสอบ

- Unit tests สำหรับ Helper functions
- Integration tests สำหรับ API calls
- E2E tests สำหรับ User flows
- Visual regression tests

## ตัวอย่างการใช้งาน

```vue
<template>
  <!-- ในหน้าข่าวสาร -->
  <div class="news-card">
    <h3>{{ news.title }}</h3>
    <p>{{ news.excerpt }}</p>

    <!-- ปุ่มอ่านต่อสำหรับข่าวที่เป็นรูปภาพ -->
    <router-link
      v-if="isImageNews(news)"
      :to="{ name: 'news-image-detail', params: { id: news.id } }"
      class="btn-read-more"
    >
      อ่านต่อ
    </router-link>

    <!-- ปุ่มอ่านต่อสำหรับข่าวทั่วไป -->
    <router-link
      v-else
      :to="{ name: 'news-detail', params: { id: news.id } }"
      class="btn-read-more"
    >
      อ่านต่อ
    </router-link>
  </div>
</template>

<script setup>
function isImageNews(news) {
  return news.imageUrl && /\.(jpg|jpeg|png|gif|webp)$/i.test(news.imageUrl)
}
</script>
```

## สรุป

หน้า `NewsImageDetailView` เป็นการปรับปรุงที่สำคัญสำหรับการแสดงข่าวสาร ให้ประสบการณ์ที่ดีขึ้นสำหรับผู้ใช้ทุกอุปกรณ์ และสร้างความน่าเชื่อถือให้กับเว็บไซต์
