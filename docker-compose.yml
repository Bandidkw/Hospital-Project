version: '3.8'
services:
  frontend:
    build:
      context: .
      target: production-stage
    ports:
      - "3001:3001"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
